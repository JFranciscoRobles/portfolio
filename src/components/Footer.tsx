import React from "react";

type Props = {};

function Footer({}: Props) {
  return (
    <div className="flex flex-row-reverse">
      <span className="text-sm font-ligth">Github code</span>
    </div>
  );
}

export default Footer;
